[tool.poetry]
name = "simple-calculator"
version = "0.1.0"
description = "A simple calculator with a safe expression evaluator."
authors = ["Nikhil Tomar <nikhiltomar2712@gmail.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
